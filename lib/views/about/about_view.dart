import 'package:flutter/material.dart';

class AboutView extends StatelessWidget {
  final String title;
  const AboutView(this.title);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}